#include <stdio.h>

int main() 
{
    int length=10, width=5, perimeter, area;
    perimeter=(length+width) *2;
    area=(length* width);
    printf("\n perimeter=%d",     perimeter);
    printf("\n area = %d" , area);
    return 0;
}

